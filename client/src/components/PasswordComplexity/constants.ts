export const HINTS: Record<string, string[]> = {
  lowercase: ['conditionLowerCase', 'At least 1 lowercase'],
  uppercase: ['conditionUpperCase', 'At least 1 uppercase'],
  digits: ['conditionNumber', 'At least 1 number'],
  symbols: ['conditionSpecialCha', 'At least 1 special character'],
  min: ['conditionMinCha', 'At least 8 characters'],
}
